= render "julia_editor"
= render "result"

:javascript
  $(document).ready(function() {

    // Allow code submission on CTRL + Enter
    $(document).keydown(function(e) {
      // Todo: Test IE compatibility:
      // var checkWebkitandIE=(e.which==26 ? 1 : 0);
      // var checkMoz=(e.which==122 && e.ctrlKey ? 1 : 0);

      if(e.metaKey) {
        if(e.which == 13) {
          // Run...
          $("#repl_form" ).submit()
        }
      }
    });
  });
